// Pode Ser completamente Alterado e criar mais so os Mantenham nessa pasta e se atente as comentarios!!!!
// script.js

// Adiciona um listener para o evento DOMContentLoaded
// Isso garante que o código dentro desta função só será executado
// após todo o conteúdo HTML da página ter sido completamente carregado.
document.addEventListener("DOMContentLoaded", function() {
    // Exibe uma mensagem no console indicando que a página foi carregada com sucesso
    console.log("Página inicial carregada com sucesso!");

    // Aqui você pode adicionar interações mais tarde, se necessário.
    // Por exemplo, você pode adicionar event listeners para botões, etc.
});
